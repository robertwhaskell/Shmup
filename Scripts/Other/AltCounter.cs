using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class AltCounter : MonoBehaviour {

	// Use this for initialization
	void Start () {
		GetComponent<Text> ().text = 0.ToString();
	}
	
	// Update is called once per frame
	void Update () {

	}
}
